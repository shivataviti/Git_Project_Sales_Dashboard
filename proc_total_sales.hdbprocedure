PROCEDURE "salesproject.db::proc_total_sales" ()
LANGUAGE SQLSCRIPT
SQL SECURITY INVOKER
AS
BEGIN
  SELECT CUSTOMER_ID, SUM(AMOUNT) AS TOTAL_SALES
  FROM "sales_order_items"
  GROUP BY CUSTOMER_ID;
END;